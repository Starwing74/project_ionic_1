import { BoutonFiltrePipe } from './bouton-filtre.pipe';

describe('BoutonFiltrePipe', () => {
  it('create an instance', () => {
    const pipe = new BoutonFiltrePipe();
    expect(pipe).toBeTruthy();
  });
});
